name: Windows RDP Session

on:
  workflow_dispatch:
    inputs:
      duration_hours:
        description: 'Durasi sesi dalam jam (maksimal 6)'
        required: false
        default: '6'

jobs:
  build-and-remote:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Konfigurasi Service RDP, Anti-Disconnect & Akselerasi Grafis
        shell: powershell
        run: |
          # 1. Buka firewall dan set otentikasi RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "fPromptForPassword" -Value 0
          Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server' -Name "fSingleSessionPerUser" -Value 1
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

          # 2. Aktifkan WDDM & Adapter Hardware Virtual untuk RDP Direct3D
          $tsPolicies = 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services'
          if (-not (Test-Path $tsPolicies)) { New-Item -Path$tsPolicies -Force | Out-Null }
          Set-ItemProperty -Path $tsPolicies -Name "bEnumerateHardwareAdapters" -Value 1
          Set-ItemProperty -Path $tsPolicies -Name "fEnableWDDMDriver" -Value 1
          Set-ItemProperty -Path $tsPolicies -Name "EnableGPUOverride" -Value 1

          # 3. Kebijakan timeout sesi agar display adapter tidak dilepas
          Set-ItemProperty -Path $tsPolicies -Name "MaxDisconnectionTime" -Value 0
          Set-ItemProperty -Path $tsPolicies -Name "MaxIdleTime" -Value 0
          Set-ItemProperty -Path $tsPolicies -Name "fResetBroken" -Value 0

      - name: Pasang DirectX 9 Runtime & Aktifkan DirectPlay
        shell: powershell
        run: |
          Write-Host "Mengaktifkan DirectPlay..."
          dism /online /enable-feature /featurename:DirectPlay /all /norestart

          Write-Host "Menginstal DirectX End-User Runtimes (June 2010)..."
          choco install directx -y --no-progress

      - name: Injeksi Default Registry Resolusi Ragnarok
        shell: powershell
        run: |
          # Mencegah error 'Cannot init d3d' akibat registry resolusi kosong (0x0)[cite: 3]
          $regPaths = @(
              "HKCU:\Software\Gravity Soft\Ragnarok",
              "HKLM:\SOFTWARE\WOW6432Node\Gravity Soft\Ragnarok"
          )
          foreach ($path in$regPaths) {
              if (-not (Test-Path $path)) { New-Item -Path$path -Force | Out-Null }
              Set-ItemProperty -Path $path -Name "WIDTH" -Value 1024
              Set-ItemProperty -Path $path -Name "HEIGHT" -Value 768
              Set-ItemProperty -Path $path -Name "BITPERPEL" -Value 32
              Set-ItemProperty -Path $path -Name "WINDOWMODE" -Value 1
              Set-ItemProperty -Path $path -Name "DEVICECNT" -Value 1
          }

      - name: Set Password Akun Runner & Siapkan Safe-Disconnect
        shell: powershell
        run: |
          $pass = ConvertTo-SecureString "${{ secrets.RDP_PASSWORD }}" -AsPlainText -Force
          Set-LocalUser -Name "runneradmin" -Password $pass

          $desktopPath = "C:\Users\runneradmin\Desktop"
          if (-not (Test-Path $desktopPath)) { New-Item -ItemType Directory -Path$desktopPath -Force | Out-Null }
          
          # Script agar display adapter tidak crash saat koneksi RDP ditutup
          $batchContent = @"
          @echo off
          for /f "skip=1 tokens=3" %%s in ('query user %USERNAME%') do (
              %windir%\System32\tscon.exe %%s /dest:console
          )
          "@
          Set-Content -Path "$desktopPath\Safe-Disconnect.bat" -Value $batchContent

      - name: Unduh dan Ekstrak dgVoodoo2 ke Desktop
        shell: powershell
        run: |
          $desktop = "C:\Users\runneradmin\Desktop"
          $targetDir = "$desktop\dgVoodoo2"
          New-Item -ItemType Directory -Path $targetDir -Force | Out-Null

          Write-Host "Mengunduh rilis dgVoodoo2 terbaru..."
          $release = Invoke-RestMethod -Uri "https://api.github.com/repos/dege-diosg/dgVoodoo2/releases/latest"
          $zipUrl = ($release.assets | Where-Object { $_.name -match '^dgVoodoo2_.*\.zip$' } | Select-Object -First 1).browser_download_url

          Invoke-WebRequest -Uri $zipUrl -OutFile "$desktop\dgVoodoo.zip"
          Expand-Archive -Path "$desktop\dgVoodoo.zip" -DestinationPath "$desktop\temp_extract" -Force

          Copy-Item "$desktop\temp_extract\MS\x86\*" -Destination $targetDir -Recurse -Force
          Copy-Item "$desktop\temp_extract\dgVoodooCpl.exe" -Destination $targetDir -Force
          Remove-Item "$desktop\dgVoodoo.zip", "$desktop\temp_extract" -Recurse -Force

      - name: Unduh, Pasang Ragnarok Zero & Launch Game
        shell: powershell
        run: |
          $installDir = "C:\Gravity\RagnarokZero"
          $desktopPath = "C:\Users\runneradmin\Desktop"
          $setupExe = "$desktopPath\RAG_SETUP_20260702.exe"
          $setupUrl = "https://patch-roz.mygnjoy.com/live/Downloader/RAG_SETUP_20260702.exe"

          Write-Host "1. Mengunduh installer Ragnarok Zero ke Desktop..."
          Invoke-WebRequest -Uri $setupUrl -OutFile$setupExe

          Write-Host "2. Menginstal ke $installDir..."
          if (-not (Test-Path $installDir)) { New-Item -ItemType Directory -Path$installDir -Force | Out-Null }
          
          # Eksekusi installer mode silent (Inno Setup / NSIS)
          Start-Process -FilePath $setupExe -ArgumentList "/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /DIR=`"$installDir`"" -Wait

          Write-Host "3. Injeksi file wrapper dgVoodoo2 ke folder game..."
          $dgVoodooDir = "$desktopPath\dgVoodoo2"
          if (Test-Path $dgVoodooDir) {
              Copy-Item "$dgVoodooDir\*" -Destination $installDir -Recurse -Force
          }

          Write-Host "4. Siapkan launcher di Desktop & Launch via PowerShell..."
          $launcherBat = @"
          @echo off
          cd /d "$installDir"
          start "" "ROZ Global.exe"
          "@
          Set-Content -Path "$desktopPath\Play-RagnarokZero.bat" -Value $launcherBat

          # Jalankan executable game
          $rozExe = "$installDir\ROZ Global.exe"
          if (Test-Path $rozExe) {
              Write-Host "Meluncurkan $rozExe..."
              Start-Process -FilePath $rozExe -WorkingDirectory$installDir
          } else {
              Write-Host "Executable $rozExe belum ditemukan. Periksa apakah proses ekstraksi installer berbeda." -ForegroundColor Yellow
          }

      - name: Unduh, Pasang, dan Hubungkan Tailscale
        shell: powershell
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
        run: |
          Write-Host "Mengunduh Tailscale..."
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile "tailscale-setup.exe"
          Start-Process -FilePath ".\tailscale-setup.exe" -ArgumentList "/quiet" -Wait

          $tailscalePath = "C:\Program Files\Tailscale\tailscale.exe"
          Start-Service Tailscale -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 3

          Write-Host "Menghubungkan runner ke Tailnet..."
          & $tailscalePath up --authkey="$env:TAILSCALE_AUTHKEY" --hostname="github-rdp" --accept-routes --unattended --reset

          $tsIp = $null$timeout = 30
          while (-not $tsIp -and$timeout -gt 0) {
              Start-Sleep -Seconds 2
              $timeout -= 2$tsIp = (& $tailscalePath ip -4 2>$null) | Select-Object -First 1
          }

          if (-not $tsIp) {               &$tailscalePath status
              throw "Gagal mendapatkan IP Tailscale runner."
          }

          Write-Host ""
          Write-Host "=================================================" -ForegroundColor Green
          Write-Host "TAILSCALE IP    : $tsIp" -ForegroundColor Yellow
          Write-Host "HOSTNAME        : github-rdp" -ForegroundColor Yellow
          Write-Host "USERNAME        : runneradmin" -ForegroundColor Yellow
          Write-Host "PASSWORD        : [Sesuai secret RDP_PASSWORD]" -ForegroundColor Yellow
          Write-Host "=================================================" -ForegroundColor Green
          Write-Host ""
          Write-Host "Koneksi via laptop/HP: mstsc /v:$tsIp" -ForegroundColor Cyan

      - name: Menjaga Sesi Tetap Aktif (Keep-Alive)
        shell: powershell
        run: |
          $hours = [math]::Min([int]"${{ github.event.inputs.duration_hours }}", 6)
          $totalSeconds =$hours * 3600
          Write-Host "Sesi RDP aktif selama $hours jam ($totalSeconds detik)..."
          
          $elapsed = 0
          while ($elapsed -lt$totalSeconds) {
              Start-Sleep -Seconds 60
              $elapsed += 60
              Write-Host "Runner aktif: $($elapsed / 60) menit dari $($totalSeconds / 60) menit."
          }
